#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "structures.h"

void Create(Student *s, int N);
void Print(Student *s, int N);
int Count(Student *s, int N);
int CountStudents(Student *s, int N);

#endif
